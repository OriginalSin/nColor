/** 
Определение переменных
**/

const int mSize = 15; // 5;
const int kSize = (mSize - 1) / 2;
const float sigma = 0.8;    // 2.0
const float GAMMA = 2.2; // 2.2; 
float kernel[mSize];

const float level = 0.01;
